# network module contract

VPC、三可用区、public/private/data subnet、NAT/VPC endpoints、security group、flow logs。输出 subnet IDs、VPC ID 和安全组接口。

`REL-001` 实现时需要变量验证、outputs、examples、tests、security/cost notes，并保持模块不读取其他模块内部资源。
