# 贡献指南

## 分支与提交

- 从最新主分支创建短生命周期分支：`feat/FND-001-bootstrap`、`fix/API-014-idempotency`。
- 一个提交应表达一个逻辑变化；建议格式：`feat(listings): add draft creation [LIST-003]`。
- 禁止在功能提交中混入全仓格式化或无关依赖升级。

## 拉取请求最小内容

- 关联 Backlog ID 与验收条件。
- 说明用户影响、架构影响、数据迁移、回滚方式。
- 列出实际运行的测试命令及结果。
- 涉及 UI 时附桌面/移动截图；涉及 API 时附契约 diff；涉及数据库时附迁移 SQL。
- 涉及安全边界时说明威胁模型和负面测试。

## 代码审查检查点

1. 是否保持模块边界和数据主权。
2. 是否存在对象级授权或租户隔离缺口。
3. 是否具有幂等、并发和失败恢复设计。
4. 是否同步更新契约、文档和测试。
5. 是否可观测且不会记录敏感数据。
6. 是否支持中文/英文、移动端、SEO 和可访问性要求。

## 数据库变更

使用 expand → migrate/backfill → switch → contract 模式完成破坏性变更。线上阶段不得在同一发布中直接删除仍被旧代码读取的列。大表回填必须分批、可恢复、可监控。

## 依赖升级

依赖升级单独提交，记录版本、变更理由、迁移注意事项和回退版本。框架大版本升级必须有 ADR 或明确的维护任务。
