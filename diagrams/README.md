# Mermaid Diagrams

- `system-context.mmd`：系统上下文。
- `containers.mmd`：运行时容器。
- `deployment.mmd`：生产部署。
- `domain-map.mmd`：领域关系。
- `er-overview.mmd`：核心 ER 概览。
- `listing-lifecycle.mmd`：Listing 状态机。
- `publish-sequence.mmd`：发布序列。
- `search-indexing.mmd`：索引事件流。
- `homepage-layout.mmd`：首页布局。

图与对应文档/代码同变更。CI 后续可用 Mermaid CLI 验证和渲染，但生成图片不作为事实源。
