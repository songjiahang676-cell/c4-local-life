{
  "name": "@socal/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "pnpm --filter @socal/config build && pnpm --filter @socal/database build && pnpm --filter @socal/observability build",
    "dev": "tsx --env-file=../../.env watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "search:index:ensure": "tsx --env-file=../../.env src/search/ensure-listing-index.ts",
    "lint": "eslint src test --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --project @socal/worker --reporter=default"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1096.0",
    "@opensearch-project/opensearch": "2.13.0",
    "@socal/config": "workspace:*",
    "@socal/contracts": "workspace:*",
    "@socal/database": "workspace:*",
    "@socal/observability": "workspace:*",
    "bullmq": "5.80.9",
    "ioredis": "5.11.1",
    "sharp": "0.34.5"
  },
  "devDependencies": {
    "@types/node": "24.10.9",
    "tsx": "4.23.1",
    "typescript": "catalog:"
  }
}
